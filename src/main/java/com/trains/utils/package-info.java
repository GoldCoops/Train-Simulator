/**
 * The utils package
 * <p>
 * Contains GridPos, MathUtils (lerp, round to int, etc.) and Point2D (an alternative way of storing x and y values)
 * Should contain Value types and stateless helpers only
 * </p>
 */

package com.trains.utils;